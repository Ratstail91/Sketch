startup:
	run the load script
	...
end

normal:
	draw map to the screen
	click left: call lua click left --can be extened later
	key down: call lua key down --can be extended later
end

scripts:
	new map
	load map
	save map
end

I need a console sub window in the editor.

-------------------------

Console:
	KeyDown(SDLKey)
	KeyUp(SDLKey)
	
	input field
	list of lines
	
	delete lines at the top of the list
	delete lines at the bottom of the list
end

-------------------------

drawing the terminal:

args: dest, terminal, font, size, rect, line count

adjust the terminal
fill the background rect
draw each line
draw the input line

-------------------------

TerminalDraw(Terminal, pScreen, ...
TerminalDoString(Terminal, L)
