BASIC FUNCTIONALITY
No more, no less, for now.

-------------------------

--this can replace the built in loop
draw() --the drawing loop (set buy something)
drawmap() --draw the map
drawtileset() --draw the tileset
drawtile(index(from the tileset), x(on the screen), y(on the screen)) --used in the drawing loops

-------------------------

Modes:
	Normal --map only
	Terminal --terminal onscreen, to allow debugging, etc.
	Tileset --choose the tile from the tileset, only temporary in the core
	Max --bounding
end

-------------------------

Architecture (Scene System):

SceneMgr:
	Init
	Loop
	Quit
end

SceneEditor:
	MouseMotion	--call the brush script
	MouseButtonDown --call the brush script
	MouseButtonUp --call the brush script
	KeyDown --pass to the terminal
	KeyUp --pass to the terminal
	
	Draw --draw the map & terminal
end

SceneTileSelect:
	draw the tileset w/ a camera
	
	set the brush/tile
end

-------------------------

